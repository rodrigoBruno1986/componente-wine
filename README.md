# **Custom Components for vtex app**

Custom Vtex IO apps using react and graphQL 
1. clone the repo

2.  `cd limpioonline-components`

3.  `cd react`

3. ` npm install `

4.  `cd ..`

5.  `vtex link`
